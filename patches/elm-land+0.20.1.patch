diff --git a/node_modules/elm-land/src/effects.js b/node_modules/elm-land/src/effects.js
index 5c6110f..5b70dd0 100644
--- a/node_modules/elm-land/src/effects.js
+++ b/node_modules/elm-land/src/effects.js
@@ -226,6 +226,9 @@ let runServer = async (options) => {
       publicDir: join(process.cwd(), 'static'),
       envDir: process.cwd(),
       envPrefix: 'ELM_LAND_',
+      optimizeDeps: {
+        exclude: ['@electric-sql/pglite'],
+      },
       cacheDir: join(process.cwd(), '.elm-land', 'server', '.vite'),
       server: {
         host: options.host,
@@ -500,6 +503,9 @@ const build = async (config) => {
       build: { outDir: '../../dist' },
       envDir: process.cwd(),
       envPrefix: 'ELM_LAND_',
+      optimizeDeps: {
+        exclude: ['@electric-sql/pglite'],
+      },
       plugins: [
         ElmVitePlugin({
           mode: 'minify',
